# expo-router 6 (SDK 54) declares an optional peer on react-server-dom-webpack
# that resolves to a React newer than the one SDK 54 pins, so a plain `npm install`
# fails with ERESOLVE. The dependency is optional and unused here, so legacy
# resolution is the correct answer rather than forcing a React mismatch.
legacy-peer-deps=true
