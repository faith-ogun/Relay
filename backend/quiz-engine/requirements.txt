fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
# Pinned to the version this service is built and tested against. It was
# `>=1.0.0`, which let a new major land on the next deploy with no review.
google-genai==2.20.0
firebase-admin==6.5.0
